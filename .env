# Build configuration for docker compose
# Change INSTALL_CUDA to true when building on a machine with an NVIDIA GPU
# and nvidia-container-toolkit installed.
INSTALL_CUDA=true
# Architecture options: auto, x64, aarch64
ONNXRUNTIME_ARCH=auto
ONNXRUNTIME_VERSION=1.18.0